language: python
python:
  - "3.6"

install:
  - pip install flask
  - pip install -r requirements.txt

# command to run app.py
script: python app.py
notifications:
  email:
    - meets2tarun@gmail.com
